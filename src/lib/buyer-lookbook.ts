/** WhatsApp template header image ratio (1125 × 600 px). */
export const BUYER_LOOKBOOK_ASPECT_CLASS = 'aspect-[19/10]' as const;

/** Default width for lookbook cards in horizontal carousels. */
export const BUYER_LOOKBOOK_CAROUSEL_WIDTH_PX = 280;

/** Default width for product cards in horizontal carousels. */
export const BUYER_PRODUCT_CAROUSEL_WIDTH_CLASS = 'w-[178px]' as const;

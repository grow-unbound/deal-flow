import type { ReactNode } from 'react';
export { metadata } from '../../categories/layout';

export default function ProductCategoriesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
